

A -> B -> C -> 
          C.NEXT = HEAD = A 

 
A -> B -> C 
          C.NEXT = B


A -> B -> C -> D
               D.NEXT = B

               D.NEXT = null

    


A -> B -> C -> D

visited = []; 
let curr
while(curr){ 
    curr = curr.next; 
    visited.push(curr); 
}
